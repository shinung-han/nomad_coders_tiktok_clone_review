import 'package:flutter/material.dart';

class BirthayScreen extends StatelessWidget {
  const BirthayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
